sp {gp*monitor*detect*goal*achived
  (state <s> ^name gp
        ^robot <r>
        ^io.output-link <out>
        ^localization <l0>)
  (<l0> ^id -1)
  (<r> ^locId -1)
-->
   (<out> ^achived.true 1)
}
